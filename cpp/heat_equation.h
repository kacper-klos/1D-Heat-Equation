#pragma once
#include <vector>
#define data_type float

std::vector<std::vector<data_type>> simulation_init(const std::vector<data_type>&,
                                                    const data_type,
                                                    const data_type,
                                                    const data_type,
                                                    const data_type);
